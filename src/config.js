const BASE_URL = 'http://10.58.5.164:3000/products';
const API = {
  main: `${BASE_URL}/all`,
  asc: `${BASE_URL}/ascending`,
  desc: `${BASE_URL}/descending`,
  list: `${BASE_URL}/list`,
};

export default API;
